# 云服务器部署详细说明

## 📋 部署前准备

### 1. 服务器信息
- **主机名称**: cloud170685
- **系统**: CentOS 7.1
- **公网IP**: 206.119.175.36
- **SSH端口**: 64478
- **用户名**: root

### 2. 本地环境要求
- 已安装 Git
- 已安装 SSH 客户端
- 已配置 SSH 密钥（推荐）或准备使用密码登录

### 3. GitHub 仓库准备
1. 在 GitHub 上创建一个新仓库
2. 将本地代码推送到 GitHub（如果还没有）

## 🚀 首次部署步骤

### 方法一：使用一键部署脚本（推荐）

1. **配置脚本**
   - 打开 `一键部署到云服务器.sh`
   - 检查配置区域的所有配置是否正确
   - 如果还没有GitHub仓库，先创建并推送代码

2. **运行部署脚本**
   ```bash
   bash 一键部署到云服务器.sh
   ```

3. **脚本会自动完成以下工作**：
   - ✅ 检查本地环境
   - ✅ 推送代码到GitHub
   - ✅ 连接到服务器
   - ✅ 安装 Node.js、PM2、Nginx、Git
   - ✅ 克隆代码到服务器
   - ✅ 安装依赖并构建前后端
   - ✅ 配置PM2管理后端服务
   - ✅ 配置Nginx反向代理
   - ✅ 启动所有服务

4. **部署完成后访问**
   - 前端: http://206.119.175.36
   - 后端API: http://206.119.175.36/api

### 方法二：手动部署（如果脚本失败）

#### 步骤1: 连接到服务器
```bash
ssh -p 64478 root@206.119.175.36
```

#### 步骤2: 安装必要软件
```bash
# 更新系统
yum update -y

# 安装Node.js
curl -fsSL https://rpm.nodesource.com/setup_20.x | bash -
yum install -y nodejs

# 安装PM2
npm install -g pm2

# 安装Nginx
yum install -y nginx
systemctl enable nginx

# 安装Git
yum install -y git
```

#### 步骤3: 配置防火墙
```bash
firewall-cmd --permanent --add-service=http
firewall-cmd --permanent --add-service=https
firewall-cmd --reload
```

#### 步骤4: 克隆代码
```bash
mkdir -p /opt/cross-border-workflow
cd /opt/cross-border-workflow
git clone <你的GitHub仓库地址> .
```

#### 步骤5: 安装和构建后端
```bash
cd /opt/cross-border-workflow/backend
npm install --production
npm run build
```

#### 步骤6: 安装和构建前端
```bash
cd /opt/cross-border-workflow/frontend
npm install

# 创建环境变量文件
cat > .env <<EOF
VITE_API_BASE_URL=http://206.119.175.36:3000
EOF

npm run build
```

#### 步骤7: 创建数据目录
```bash
mkdir -p /opt/cross-border-workflow/backend/data
mkdir -p /opt/cross-border-workflow/backend/uploads
```

#### 步骤8: 启动后端服务（使用PM2）
```bash
cd /opt/cross-border-workflow/backend
pm2 start dist/index.js --name cross-border-workflow --env production
pm2 set cross-border-workflow PORT 3000
pm2 save
pm2 startup systemd -u root --hp /root
```

#### 步骤9: 配置Nginx
```bash
cat > /etc/nginx/conf.d/cross-border-workflow.conf <<EOF
server {
    listen 80;
    server_name _;

    client_max_body_size 30M;

    # 前端静态文件
    location / {
        root /opt/cross-border-workflow/frontend/dist;
        try_files \$uri \$uri/ /index.html;
        index index.html;
    }

    # API 代理
    location /api {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade \$http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host \$host;
        proxy_set_header X-Real-IP \$remote_addr;
        proxy_set_header X-Forwarded-For \$proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto \$scheme;
        proxy_cache_bypass \$http_upgrade;
    }

    # 上传文件访问
    location /uploads {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Host \$host;
    }
}
EOF

# 测试配置
nginx -t

# 启动Nginx
systemctl restart nginx
```

## 🔄 更新代码

### 使用一键更新脚本（推荐）

1. **在本地修改代码**

2. **运行更新脚本**
   ```bash
   bash 一键更新.sh
   ```

3. **脚本会自动完成**：
   - ✅ 提交并推送代码到GitHub
   - ✅ 在服务器上拉取最新代码
   - ✅ 重新安装依赖
   - ✅ 重新构建前后端
   - ✅ 重启服务

### 手动更新

1. **在本地提交并推送代码**
   ```bash
   git add .
   git commit -m "更新说明"
   git push origin main
   ```

2. **在服务器上更新**
   ```bash
   ssh -p 64478 root@206.119.175.36
   cd /opt/cross-border-workflow
   git pull origin main
   
   # 更新后端
   cd backend
   npm install --production
   npm run build
   pm2 restart cross-border-workflow
   
   # 更新前端
   cd ../frontend
   npm install
   npm run build
   systemctl reload nginx
   ```

## 🔧 常用管理命令

### 查看服务状态
```bash
# 查看PM2进程
pm2 status
pm2 logs cross-border-workflow

# 查看Nginx状态
systemctl status nginx
```

### 重启服务
```bash
# 重启后端
pm2 restart cross-border-workflow

# 重启Nginx
systemctl restart nginx
```

### 查看日志
```bash
# 后端日志
pm2 logs cross-border-workflow

# Nginx错误日志
tail -f /var/log/nginx/error.log

# Nginx访问日志
tail -f /var/log/nginx/access.log
```

### 停止服务
```bash
# 停止后端
pm2 stop cross-border-workflow

# 停止Nginx
systemctl stop nginx
```

## 🐛 故障排查

### 1. 无法访问网站
- 检查防火墙是否开放80端口
- 检查Nginx是否运行: `systemctl status nginx`
- 检查后端是否运行: `pm2 status`

### 2. 前端显示空白
- 检查前端构建是否成功
- 检查Nginx配置中的路径是否正确
- 查看浏览器控制台错误信息

### 3. API请求失败
- 检查后端服务是否运行: `pm2 logs cross-border-workflow`
- 检查后端端口是否正确（应该是3000）
- 检查Nginx代理配置

### 4. 文件上传失败
- 检查uploads目录权限
- 检查Nginx的client_max_body_size配置

## 📝 注意事项

1. **端口说明**：
   - 前端通过Nginx在80端口提供服务
   - 后端运行在3000端口（内部）
   - 用户访问时只需要访问80端口

2. **数据持久化**：
   - 数据存储在 `/opt/cross-border-workflow/backend/data`
   - 上传文件存储在 `/opt/cross-border-workflow/backend/uploads`
   - 建议定期备份这些目录

3. **安全建议**：
   - 配置SSH密钥登录（禁用密码登录）
   - 定期更新系统和软件
   - 配置HTTPS（需要域名和SSL证书）

4. **性能优化**：
   - PM2已配置为生产模式
   - 可以根据需要调整PM2实例数量
   - 可以配置Nginx缓存提升性能

## 🎯 下一步

部署完成后，你可以：
1. 访问 http://206.119.175.36 查看网站
2. 注册账号并开始使用
3. 根据需要配置环境变量（API密钥等）
4. 定期备份数据

如有问题，请查看日志文件或联系技术支持。
