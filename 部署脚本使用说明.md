# 部署脚本使用说明

## 一、一键推送到 GitHub

### 脚本：`一键推送.sh`

**功能**：
- 自动检测代码更改
- 自动提交并推送到 GitHub
- 支持自定义提交信息

**使用方法**：

```bash
# 基本用法（使用默认提交信息）
./一键推送.sh

# 自定义提交信息
./一键推送.sh "修复了某个bug"

# 自动模式（跳过确认，用于 CI/CD）
./一键推送.sh "更新代码" auto
```

**示例**：
```bash
# 提交并推送
./一键推送.sh "添加新功能"

# 自动模式（无需确认）
./一键推送.sh "自动更新" auto
```

---

## 二、令牌认证部署脚本

### 脚本：`一键部署-令牌版.sh`

**功能**：
- 支持 SSH 密钥认证
- 支持密码认证（需要 sshpass）
- 支持 GitHub Token 拉取代码
- 支持环境变量配置

**使用方法**：

#### 方法 1：使用 SSH 密钥（推荐）

```bash
# 设置环境变量
export SSH_KEY_PATH=~/.ssh/id_rsa
export GITHUB_TOKEN=ghp_xxxxxxxxxxxxx  # 可选，用于私有仓库

# 首次部署
./一键部署-令牌版.sh init

# 更新部署
./一键部署-令牌版.sh update
```

#### 方法 2：使用密码认证

```bash
# 设置环境变量
export SERVER_PASSWORD=your_password
export GITHUB_TOKEN=ghp_xxxxxxxxxxxxx  # 可选

# 首次部署
./一键部署-令牌版.sh init

# 更新部署
./一键部署-令牌版.sh update
```

#### 方法 3：交互式输入

```bash
# 直接运行，脚本会提示输入
./一键部署-令牌版.sh init
```

**可用命令**：
- `init` - 首次部署（安装 Docker、克隆代码、启动）
- `update` - 更新部署（拉取最新代码并重启）
- `status` - 查看服务状态
- `logs` - 查看日志
- `restart` - 重启服务
- `stop` - 停止服务
- `ssh` - 连接到服务器

**环境变量配置**：

```bash
# 服务器配置
export SERVER_IP=206.119.175.36
export SERVER_PORT=64478
export SERVER_USER=root

# 认证配置
export SSH_KEY_PATH=~/.ssh/id_rsa
export SERVER_PASSWORD=your_password
export GITHUB_TOKEN=ghp_xxxxxxxxxxxxx

# 项目配置
export GITHUB_REPO=https://github.com/jinghoor/baodangongzuoliu.git
export PROJECT_DIR=/root/baodangongzuoliu
```

**完整示例**：

```bash
# 1. 配置环境变量
export SSH_KEY_PATH=~/.ssh/id_rsa
export GITHUB_TOKEN=ghp_xxxxxxxxxxxxx

# 2. 首次部署
./一键部署-令牌版.sh init

# 3. 之后每次更新
./一键部署-令牌版.sh update
```

---

## 三、GitHub Actions 自动部署

### 配置步骤

1. **在 GitHub 仓库设置 Secrets**：
   - 进入仓库 Settings → Secrets and variables → Actions
   - 添加以下 Secrets：
     - `SERVER_IP`: 服务器 IP 地址
     - `SERVER_PORT`: SSH 端口（如 64478）
     - `SERVER_USER`: 用户名（如 root）
     - `SSH_PRIVATE_KEY`: SSH 私钥内容
     - `PROJECT_DIR`: 项目目录（如 /root/baodangongzuoliu）

2. **推送代码后自动部署**：
   - 代码推送到 `main` 或 `master` 分支后，GitHub Actions 会自动触发部署
   - 也可以手动触发：Actions → 选择工作流 → Run workflow

3. **查看部署日志**：
   - 在 GitHub 仓库的 Actions 标签页查看部署日志

**注意事项**：
- SSH 私钥需要包含完整的密钥内容（包括 `-----BEGIN` 和 `-----END` 行）
- 确保服务器允许从 GitHub Actions 的 IP 范围连接

---

## 四、完整工作流程

### 推荐流程（使用令牌认证）

```bash
# 1. 本地修改代码后，一键推送
./一键推送.sh "更新功能"

# 2. 自动部署到服务器（使用 SSH 密钥）
export SSH_KEY_PATH=~/.ssh/id_rsa
./一键部署-令牌版.sh update
```

### 或者使用 GitHub Actions（完全自动化）

```bash
# 1. 本地修改代码后，一键推送
./一键推送.sh "更新功能"

# 2. GitHub Actions 会自动部署（无需手动操作）
```

---

## 五、常见问题

### Q1: 推送时提示权限错误
**A**: 检查 GitHub Token 是否有推送权限，或使用 SSH 方式配置远程仓库。

### Q2: 部署时连接服务器失败
**A**: 
- 检查服务器 IP 和端口是否正确
- 检查 SSH 密钥或密码是否正确
- 检查防火墙是否开放相应端口

### Q3: 使用密码认证时提示需要 sshpass
**A**: 
```bash
# macOS
brew install sshpass

# Ubuntu/Debian
sudo apt-get install sshpass
```

### Q4: GitHub Actions 部署失败
**A**: 
- 检查 Secrets 配置是否正确
- 检查 SSH 私钥格式是否正确（需要包含完整内容）
- 检查服务器是否允许 GitHub Actions IP 连接

---

## 六、安全建议

1. **不要将敏感信息提交到代码仓库**
   - 使用环境变量或 GitHub Secrets
   - 将 `.env` 文件添加到 `.gitignore`

2. **使用 SSH 密钥而非密码**
   - 更安全
   - 无需每次输入密码

3. **定期轮换 Token 和密钥**
   - GitHub Token 定期更新
   - SSH 密钥定期更换

4. **限制服务器访问**
   - 使用防火墙限制 SSH 端口访问
   - 使用 fail2ban 防止暴力破解

---

*最后更新：2026年1月27日*
