# 手动连接服务器并部署完整指南

## 一、连接服务器

### 方法 1：使用 SSH 密码连接

在终端执行：

```bash
ssh -p 64478 root@206.119.175.36
```

系统会提示输入密码，输入服务器 root 密码即可。

### 方法 2：如果 SSH 被限制，使用云服务商控制台

1. 登录云服务商控制台（如阿里云、腾讯云等）
2. 找到服务器实例
3. 点击"远程连接"或"VNC"登录

---

## 二、连接后的部署步骤

### 步骤 1：进入项目目录

```bash
cd /root/baodangongzuoliu
```

**如果目录不存在**，需要先克隆项目：

```bash
cd /root
git clone https://github.com/jinghoor/baodangongzuoliu.git
cd baodangongzuoliu
```

### 步骤 2：拉取最新代码

```bash
# 获取远程最新代码
git fetch origin main

# 重置到最新版本（会覆盖本地修改）
git reset --hard origin/main
```

**说明**：
- `git fetch` - 从 GitHub 获取最新代码，但不合并
- `git reset --hard` - 强制重置到远程 main 分支，丢弃所有本地修改

### 步骤 3：停止旧容器

```bash
docker compose down
```

这会停止并删除当前运行的容器。

### 步骤 4：构建新镜像

```bash
docker compose build --no-cache
```

**说明**：
- `--no-cache` - 不使用缓存，确保使用最新代码构建

### 步骤 5：启动服务

```bash
docker compose up -d
```

**说明**：
- `-d` - 后台运行（detached mode）

### 步骤 6：查看服务状态

```bash
docker compose ps
```

应该看到容器正在运行。

---

## 三、一键执行（推荐）

您也可以将以上步骤合并为一条命令执行：

```bash
cd /root/baodangongzuoliu && \
git fetch origin main && \
git reset --hard origin/main && \
docker compose down && \
docker compose build --no-cache && \
docker compose up -d && \
docker compose ps
```

**复制上面的命令，在服务器上直接粘贴执行即可！**

---

## 四、验证部署

### 1. 检查容器状态

```bash
docker compose ps
```

应该看到 `cross-border-workflow` 容器状态为 `Up`。

### 2. 查看日志

```bash
docker compose logs -f
```

按 `Ctrl+C` 退出日志查看。

### 3. 访问网站

在浏览器访问：http://206.119.175.36

---

## 五、常见问题

### 问题 1：git fetch 失败

**错误**：`fatal: not a git repository`

**解决**：确保在正确的目录，或重新克隆项目：

```bash
cd /root
rm -rf baodangongzuoliu
git clone https://github.com/jinghoor/baodangongzuoliu.git
cd baodangongzuoliu
```

### 问题 2：docker compose 命令不存在

**错误**：`command not found: docker compose`

**解决**：使用 `docker-compose`（注意有横线）：

```bash
docker-compose down
docker-compose build --no-cache
docker-compose up -d
```

### 问题 3：构建失败

**解决**：查看详细错误信息：

```bash
docker compose build --no-cache 2>&1 | tee build.log
```

然后查看 `build.log` 文件找出问题。

### 问题 4：端口被占用

**解决**：检查端口占用：

```bash
netstat -tulpn | grep 80
# 或
lsof -i :80
```

如果端口被占用，停止占用端口的服务或修改 `docker-compose.yml` 中的端口映射。

---

## 六、快速参考

### 完整部署命令（复制使用）

```bash
cd /root/baodangongzuoliu && git fetch origin main && git reset --hard origin/main && docker compose down && docker compose build --no-cache && docker compose up -d && docker compose ps
```

### 仅更新代码（不重建镜像）

```bash
cd /root/baodangongzuoliu && git fetch origin main && git reset --hard origin/main && docker compose restart
```

### 查看日志

```bash
cd /root/baodangongzuoliu && docker compose logs -f
```

### 停止服务

```bash
cd /root/baodangongzuoliu && docker compose down
```

### 重启服务

```bash
cd /root/baodangongzuoliu && docker compose restart
```

---

## 七、部署流程图

```
1. 连接服务器
   ↓
2. cd /root/baodangongzuoliu
   ↓
3. git fetch origin main
   ↓
4. git reset --hard origin/main
   ↓
5. docker compose down
   ↓
6. docker compose build --no-cache
   ↓
7. docker compose up -d
   ↓
8. docker compose ps (验证)
   ↓
完成！
```

---

*最后更新：2026年1月27日*
