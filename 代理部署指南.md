# 通过 SOCKS5 代理部署指南

## 代理信息

- **类型**: SOCKS5
- **地址**: 103.219.103.199
- **端口**: 28826
- **用户名**: 14affc5de9939
- **密码**: e40b81193e

---

## 方法 1：使用 proxychains（推荐）

### 1. 安装 proxychains

**macOS**:
```bash
brew install proxychains-ng
```

**Ubuntu/Debian**:
```bash
sudo apt-get install proxychains4
```

### 2. 配置 proxychains

编辑配置文件（macOS 通常在 `~/.proxychains/proxychains.conf` 或 `/usr/local/etc/proxychains.conf`）：

```bash
# macOS
nano ~/.proxychains/proxychains.conf
# 或
nano /usr/local/etc/proxychains.conf

# Ubuntu
sudo nano /etc/proxychains.conf
```

在文件末尾的 `[ProxyList]` 部分添加：

```
socks5 103.219.103.199 28826 14affc5de9939 e40b81193e
```

### 3. 使用代理部署

```bash
# 测试连接
proxychains4 ssh -p 64478 root@206.119.175.36 "echo '连接成功'"

# 执行部署
./一键部署-代理版.sh update
```

---

## 方法 2：手动通过代理连接

### 步骤 1：连接服务器（通过代理）

```bash
# 使用 proxychains
proxychains4 ssh -p 64478 root@206.119.175.36
```

### 步骤 2：在服务器上执行部署

连接成功后，执行：

```bash
cd /root/baodangongzuoliu && \
git fetch origin main && \
git reset --hard origin/main && \
docker compose down && \
docker compose build --no-cache && \
docker compose up -d && \
docker compose ps
```

---

## 方法 3：使用 SSH ProxyCommand（需要支持认证的 netcat）

如果您的系统有支持 SOCKS5 认证的 netcat，可以使用：

```bash
ssh -o ProxyCommand="nc -X 5 -x 103.219.103.199:28826 %h %p" \
    -p 64478 root@206.119.175.36
```

**注意**：标准 netcat 可能不支持 SOCKS5 用户名密码认证，建议使用方法 1。

---

## 快速部署命令

### 一键部署脚本

```bash
# 测试代理连接
./一键部署-代理版.sh test

# 更新部署
./一键部署-代理版.sh update
```

### 手动部署（通过代理）

```bash
# 1. 连接服务器
proxychains4 ssh -p 64478 root@206.119.175.36

# 2. 执行部署（在服务器上）
cd /root/baodangongzuoliu && \
git fetch origin main && \
git reset --hard origin/main && \
docker compose down && \
docker compose build --no-cache && \
docker compose up -d && \
docker compose ps
```

---

## 验证代理连接

### 测试 1：测试代理是否工作

```bash
proxychains4 curl -I https://www.google.com
```

如果返回 HTTP 响应，说明代理工作正常。

### 测试 2：测试 SSH 连接

```bash
proxychains4 ssh -p 64478 -o ConnectTimeout=10 root@206.119.175.36 "echo 'SSH连接成功'"
```

---

## 常见问题

### 问题 1：proxychains 命令不存在

**解决**：
```bash
# macOS
brew install proxychains-ng

# Ubuntu
sudo apt-get install proxychains4
```

### 问题 2：代理连接失败

**检查**：
1. 代理地址和端口是否正确
2. 用户名和密码是否正确
3. 代理服务器是否在线
4. 防火墙是否允许连接

### 问题 3：SSH 连接超时

**解决**：
- 增加超时时间：`-o ConnectTimeout=30`
- 检查代理配置是否正确
- 尝试直接测试代理：`proxychains4 curl -I https://www.google.com`

---

## 代理配置文件示例

### proxychains.conf（完整示例）

```
strict_chain
proxy_dns
remote_dns_subnet 224
tcp_read_time_out 15000
tcp_connect_time_out 8000

[ProxyList]
socks5 103.219.103.199 28826 14affc5de9939 e40b81193e
```

---

*最后更新：2026年1月27日*
