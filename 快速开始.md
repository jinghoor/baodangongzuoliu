# 🚀 快速开始 - 一键部署指南

## 📝 第一步：准备GitHub仓库

1. **在GitHub上创建新仓库**
   - 访问 https://github.com/new
   - 创建新仓库（例如：`cross-border-workflow`）
   - 不要初始化README、.gitignore或license

2. **推送代码到GitHub**
   ```bash
   # 如果还没有初始化git
   git init
   git add .
   git commit -m "Initial commit"
   
   # 添加远程仓库
   git remote add origin https://github.com/你的用户名/你的仓库名.git
   
   # 推送代码
   git branch -M main
   git push -u origin main
   ```

## 🎯 第二步：配置部署脚本

1. **打开 `一键部署到云服务器.sh`**
   - 找到配置区域（文件开头）
   - 修改 `GITHUB_REPO` 为你的GitHub仓库地址
   - 例如：`GITHUB_REPO="https://github.com/yourusername/your-repo.git"`

2. **检查其他配置**（通常不需要修改）
   - `SERVER_IP`: 206.119.175.36 ✅
   - `SERVER_PORT`: 64478 ✅
   - `SERVER_USER`: root ✅
   - `BACKEND_PORT`: 3000 ✅
   - `FRONTEND_PORT`: 80 ✅

## 🚀 第三步：执行一键部署

```bash
bash 一键部署到云服务器.sh
```

**脚本会自动完成：**
- ✅ 检查本地环境
- ✅ 推送代码到GitHub
- ✅ 连接到服务器
- ✅ 安装所有必要软件（Node.js, PM2, Nginx等）
- ✅ 部署前后端代码
- ✅ 配置并启动所有服务

**预计时间：** 5-10分钟（取决于网络速度）

## ✅ 第四步：验证部署

部署完成后，访问：
- **网站地址**: http://206.119.175.36
- **API地址**: http://206.119.175.36/api

如果看到登录页面，说明部署成功！🎉

## 🔄 后续更新代码

当你修改了代码后，只需要运行：

```bash
bash 一键更新.sh
```

脚本会自动：
- ✅ 提交并推送代码到GitHub
- ✅ 在服务器上拉取最新代码
- ✅ 重新构建前后端
- ✅ 重启服务

## ❓ 常见问题

### Q1: 脚本执行失败，提示"Permission denied"
**解决方案：**
```bash
chmod +x 一键部署到云服务器.sh
chmod +x 一键更新.sh
```

### Q2: 无法连接到服务器
**检查：**
- SSH端口是否正确（64478）
- 服务器IP是否正确（206.119.175.36）
- 网络是否正常

### Q3: GitHub推送失败
**解决方案：**
- 确保已配置GitHub账号
- 检查仓库地址是否正确
- 如果使用HTTPS，可能需要输入用户名和密码

### Q4: 部署后无法访问网站
**检查：**
- 服务器防火墙是否开放80端口
- 运行 `pm2 status` 查看后端是否运行
- 运行 `systemctl status nginx` 查看Nginx是否运行

### Q5: 如何查看日志
**在服务器上运行：**
```bash
# 后端日志
pm2 logs cross-border-workflow

# Nginx日志
tail -f /var/log/nginx/error.log
```

## 📚 更多信息

详细说明请查看 `部署说明.md`

## 🆘 需要帮助？

如果遇到问题：
1. 查看 `部署说明.md` 中的故障排查部分
2. 检查服务器日志
3. 确认所有配置是否正确

---

**祝你部署顺利！** 🎊
