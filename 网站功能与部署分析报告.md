# 网站功能与部署分析报告

## 一、网站功能概览

### 1.1 核心功能
这是一个**可视化工作流系统**（爆单工作流），主要用于跨境电商工作流自动化。

**技术栈：**
- **前端**：React + Vite + TypeScript + React Flow
- **后端**：Express.js + TypeScript
- **部署**：Docker + Docker Compose
- **数据存储**：JSON 文件（本地存储）

### 1.2 主要功能模块

#### 用户系统
- ✅ 用户注册/登录/登出
- ✅ 用户认证（基于 Token）
- ✅ 个人资料管理
- ✅ 积分系统（积分消费和充值）
- ✅ 管理员功能（用户管理、积分管理）

#### 工作流管理
- ✅ 创建/编辑/删除工作流
- ✅ 工作流复制
- ✅ 工作流分享（JSON 格式）
- ✅ 工作流导入
- ✅ 工作流执行（同步 DAG 执行）
- ✅ 运行日志查看

#### 节点类型支持
- **触发器**：开始节点、定时任务（Cron）、Webhook
- **LLM 节点**：支持 OpenAI 兼容 API、Doubao-Seed-1.8 多模态模型
- **文本处理**：文本输入/输出、模板变量替换
- **图片处理**：图片输入/输出、base64 转换
- **控制流**：条件判断、循环
- **HTTP 请求**：GET/POST 等，支持模板
- **文件操作**：文件保存、上传
- **其他**：时间节点、日志节点、显示节点

#### 其他功能
- ✅ 文件上传（支持多文件，最大 30MB）
- ✅ Provider 配置管理（自定义 API 端点）
- ✅ 运行历史记录
- ✅ 实时日志查看

### 1.3 访问地址
- **生产环境**：http://206.119.175.36
- **开发环境**：http://localhost:1888（后端），http://localhost:5173（前端）

---

## 二、当前部署方案分析

### 2.1 一键推送到 GitHub

**现状：**
- ✅ 存在 `push-to-github.sh` 脚本
- ❌ 需要手动交互确认（不是完全自动化）
- ❌ 不支持自动提交信息

**脚本位置**：`./push-to-github.sh`

**使用方式**：
```bash
./push-to-github.sh
# 需要手动确认是否推送
```

**改进建议**：
1. 支持自动推送（无需确认）
2. 支持自定义提交信息
3. 支持自动检测更改并提交

### 2.2 云服务器部署

**现状：**
- ✅ 存在 `一键部署.sh` 脚本
- ✅ 支持首次部署（init）和更新部署（update）
- ❌ **使用 SSH 密钥认证，不支持令牌认证**
- ❌ 需要预先配置 SSH 免密登录

**服务器信息**：
- IP：206.119.175.36
- SSH 端口：64478
- 用户：root
- GitHub 仓库：https://github.com/jinghoor/baodangongzuoliu.git

**当前认证方式**：
```bash
ssh -p 64478 root@206.119.175.36
# 需要 SSH 密钥或密码
```

**部署命令**：
```bash
./一键部署.sh init    # 首次部署
./一键部署.sh update  # 更新部署
```

**改进建议**：
1. 支持令牌认证（GitHub Personal Access Token）
2. 支持通过环境变量配置服务器信息
3. 支持通过令牌直接拉取代码并部署

---

## 三、改进方案

### 3.1 一键推送脚本改进

**创建新脚本**：`一键推送.sh`

**功能**：
- 自动检测更改
- 自动提交（可配置提交信息）
- 自动推送到 GitHub
- 支持跳过确认（用于 CI/CD）

### 3.2 令牌认证部署方案

**方案 A：使用 GitHub Personal Access Token**

通过 GitHub Token 直接拉取代码，无需 SSH 密钥。

**方案 B：使用服务器 API Token**

如果服务器提供 API，可以通过 API Token 触发部署。

**方案 C：GitHub Actions 自动部署**

使用 GitHub Actions 在推送后自动部署到服务器。

---

## 四、推荐实现

### 4.1 改进的一键推送脚本

创建 `一键推送.sh`，支持：
- 自动检测更改
- 自动提交和推送
- 可配置提交信息

### 4.2 令牌认证部署脚本

创建 `一键部署-令牌版.sh`，支持：
- 使用 GitHub Token 拉取代码
- 通过 SSH 密码或令牌连接服务器
- 自动部署流程

### 4.3 GitHub Actions 自动部署

创建 `.github/workflows/deploy.yml`，实现：
- 代码推送到 main 分支后自动触发
- 使用 GitHub Secrets 存储敏感信息
- 自动部署到服务器

---

## 五、总结

### 当前状态
- ✅ 网站功能完整，支持可视化工作流创建和执行
- ✅ 有基本的部署脚本
- ❌ 推送需要手动确认
- ❌ 部署需要 SSH 密钥，不支持令牌认证

### 改进后
- ✅ 完全自动化的一键推送
- ✅ 支持令牌认证的部署
- ✅ 可选的 GitHub Actions 自动部署

---

*报告生成时间：2026年1月27日*
